[build]
# Serve static files from the public directory
publish = "public"
# Skip any build process - just serve static files
command = "echo 'Deploying frontend only'"
# Don't process any dependencies
ignore = "false"

# Redirect SPA requests to index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
